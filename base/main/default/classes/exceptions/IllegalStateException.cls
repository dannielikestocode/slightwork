/**
 * The IllegalStateException is used to throw exceptions
 * when executing logic on an object that would result
 * in an invalid state (E.g., Validation failure)
 * 
 * @author Dannie @ Wynforce
 * @since v66.0
 */
@NamespaceAccessible
public without sharing class IllegalStateException extends Exception { }